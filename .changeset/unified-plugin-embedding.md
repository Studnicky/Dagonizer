---
"@studnicky/dagonizer": minor
---

Add a unified plugin and embedded-DAG authoring path. `defineDagonizerPlugin()` packages reusable DAG parts behind a plugin id, `DAGBuilder.embed()` accepts registered DAG references, and the JSON-LD/builder flow remains the canonical assembly surface for reusable graph composition.
