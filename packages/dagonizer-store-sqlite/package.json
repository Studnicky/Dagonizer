{
  "name": "@studnicky/dagonizer-store-sqlite",
  "version": "2.0.0",
  "description": "SQLite-backed Store for @studnicky/dagonizer using Node's built-in node:sqlite.",
  "private": false,
  "license": "MIT",
  "author": "Andrew Studnicky <a.j.studnicky@gmail.com>",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "node": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist dist-test",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "rm -rf dist-test && tsc -p tsconfig.test.json",
    "test": "node --test 'dist-test/tests/**/*.test.js'",
    "lint": "eslint . --max-warnings 0",
    "prepack": "npm run clean && npm run build"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://npm.pkg.github.com"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@studnicky/dagonizer": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Studnicky/Dagonizer.git",
    "directory": "packages/dagonizer-store-sqlite"
  }
}
